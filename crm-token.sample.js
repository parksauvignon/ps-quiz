/* ────────────────────────────────────────────────────────────
   Токен G-Plus CRM для квіза.
   Як увімкнути відправку лідів:
     1. скопіювати цей файл поруч і назвати crm-token.js
     2. вписати всередину токен partner-custom-form
        (той самий, що вже стоїть на лендингу єОселя)
     3. залити crm-token.js на сервер разом з index.html
   У git цей файл НЕ потрапляє (див. .gitignore), тому в публічному
   репозиторії токена немає. Якщо файлу немає — квіз просто не шле
   заявку в CRM і пише про це в консоль.
   ──────────────────────────────────────────────────────────── */
window.PS_CRM_TOKEN = 'ВПИСАТИ_ТОКЕН';
